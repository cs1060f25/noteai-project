# Video Compilation Agent Dependencies
# Add these to your existing requirements.txt

# FFmpeg Python wrapper (optional, we use subprocess)
# ffmpeg-python==0.2.0

# Required for video compilation agent
boto3>=1.26.0
redis>=4.5.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-mock>=3.11.0